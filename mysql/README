These are steps required to mysql interface for scaledb:
- obtain mariadb source code
- create directory <mariadb_source>/storage/scaledb
- copy scaledb-interfaces/mysql/impl/Makefile.am into <mariadb_source>/storage/scaledb 
- copy scaledb-interfaces/mysql/impl/plug.in into <mariadb_source>/storage/scaledb 
- copy libscaledb.so (proprietary library) into <mariadb_source>/storage/scaledb
- create an interface directory <mariadb_source>/storage/scaledb/interface 
- copy mysql directory scaledb-interfaces/mysql into <mariadb_source>/storage/scaledb/interface
- copy scaledb-interfaces/scaledb/* directory into <mariadb_source>/storage/scaledb/interface
- remove the plugin.in and Makefile.am from <mariadb_source>/storage/scaledb/interface/mysql/impl
- build mysql
- run mysql with LD_LIBRARY_PATH including path to libscaledb.so ((proprietary library)
- at mysql prompt INSTALL PLUGIN scaledb SONAME 'ha_scaledb.so'
